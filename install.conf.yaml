- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig: .gitconfig
    ~/.gitconfig.windows: .gitconfig.windows
    ~/.gitconfig.local: .gitconfig.local
    ~/.zshrc: .zshrc
    ~/.bashrc: .bashrc
    ~/.vimrc: .vimrc
    ~/.p10k.zsh: .p10k.zsh
